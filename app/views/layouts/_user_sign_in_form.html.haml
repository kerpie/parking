= form_for(resource_name, url: user_session_path ) do |f|
	%div
		= f.label :email
		%br
		= f.email_field :email

	%div
		= f.label :password
		%br
		= f.password_field :password

	%div 
		= f.submit "go!"

= render "users/shared/links"